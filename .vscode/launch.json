{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Dream program",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/zig-out/bin/DreamCompiler",
      "args": ["${file}"],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build Dream",
      "stopAtEntry": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb"
    }
  ]
}
